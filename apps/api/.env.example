# ===== Required Environment Variables ======
HOST=0.0.0.0
PORT=3002
EXTRACT_WORKER_PORT=3004
QUEUE_WORKER_PORT=3005

# For self-hosting using docker, use redis://redis:6379. For running locally, use redis://localhost:6379
REDIS_URL=redis://redis:6379
REDIS_RATE_LIMIT_URL=redis://redis:6379

# For self-hosting using docker, use postgres://postgres:postgres@nuq-postgres:5432/postgres. For running locally, use postgres://postgres:postgres@localhost:5432/postgres
NUQ_DATABASE_URL=postgres://postgres:postgres@nuq-postgres:5432/postgres

# For using the Playwright microservice, set the URL here (set to empty if you are not using Playwright)
PLAYWRIGHT_MICROSERVICE_URL=http://playwright-service:3000/scrape

# LOGGING_LEVEL determines the verbosity of logs that the system will output.
# Available levels are:
# NONE - No logs will be output.
# ERROR - For logging error messages that indicate a failure in a specific operation.
# WARN - For logging potentially harmful situations that are not necessarily errors.
# INFO - For logging informational messages that highlight the progress of the application.
# DEBUG - For logging detailed information on the flow through the system, primarily used for debugging.
# TRACE - For logging more detailed information than the DEBUG level.
# Set LOGGING_LEVEL to one of the above options to control logging output.
LOGGING_LEVEL=TRACE

# To turn on DB authentication, you need to set up supabase.
# Note: DB authentication is not currently supported for self-hosted instances.
USE_DB_AUTHENTICATION=false

# ===== Optional ENVS ======

# ----- Search Providers -----

# SearchApi key. Head to https://searchapi.com/ to get your API key
SEARCHAPI_API_KEY=

# SearchApi engine, defaults to google. Available options: google, bing, baidu, google_news, etc. Head to https://searchapi.com/ to explore more engines
SEARCHAPI_ENGINE=

# If using Serper for search, set your API key here. Head to https://serper.dev/ to get your API key
SERPER_API_KEY=

# If using SearXNG self-hosted or public instance, set the endpoint here
SEARXNG_ENDPOINT=
SEARXNG_ENGINES=
SEARXNG_CATEGORIES=

# ----- LLM Providers -----

# You can modify the OPENAI_BASE_URL to point to a different endpoint 
# if you're using a proxy or a different service that is compatible with the OpenAI API.
OPENAI_BASE_URL=

# API key used for the OpenAI API provider
OPENAI_API_KEY=

ANTHROPIC_API_KEY=
GROQ_API_KEY=
OPENROUTER_API_KEY=
FIREWORKS_API_KEY=
DEEPINFRA_API_KEY=
GOOGLE_GENERATIVE_AI_API_KEY=

OLLAMA_BASE_URL=

MODEL_NAME=
MODEL_EMBEDDING_NAME=

# VERTEX_CREDENTIALS supports either a base64 encoded JSON blob or a path to a file
# Only set this if you're using Google Vertex as your LLM provider
VERTEX_CREDENTIALS=

# ----- Other Optionals -----

# Set this to the URL of your webhook when using the self-hosted version of FireCrawl
SELF_HOSTED_WEBHOOK_URL=

# Set this to the HMAC secret of your webhook when using the self-hosted version of FireCrawl
SELF_HOSTED_WEBHOOK_HMAC_SECRET=

# Set this to true to allow local webhooks (e.g. http://localhost:3000/webhook or internal IPs / hostnames)
# WARNING: This can be a security risk if you're using webhooks from untrusted sources
# as it could allow SSRF attacks to your internal network. Use with caution.
ALLOW_LOCAL_WEBHOOKS=false

# Set to true to log to a file instead of stdout. Logs will be written to /logs/firecrawl.log
FIRECRAWL_LOG_TO_FILE=true

# Use if you've set up authentication and want to test with a real API key
TEST_API_KEY=

# Set if you'd like to test the scraping rate limit
RATE_LIMIT_TEST_API_KEY_SCRAPE=

# Set if you'd like to test the crawling rate limit
RATE_LIMIT_TEST_API_KEY_CRAWL=

# Auth key for Bull dashboard, change this to a strong random value, only A-z 0-9 and symbols '-', '_', '@' are allowed
BULL_AUTH_KEY=@

# Set if you'd like to send slack server health status messages
SLACK_WEBHOOK_URL=

# Set if you'd like to send posthog events like job logs
POSTHOG_API_KEY=
POSTHOG_HOST=

# Proxy Settings for Playwright (Alternative you can can use a proxy service like oxylabs, which rotates IPs for you on every request)
PROXY_SERVER=
PROXY_USERNAME=
PROXY_PASSWORD=

# Set if you'd like to block media requests to save proxy bandwidth
BLOCK_MEDIA=

# Resend API Key for transactional emails
RESEND_API_KEY=

# x402 Payment Configuration
X402_ENABLED=true
X402_PAY_TO_ADDRESS=
                    # ^ Address taken from -> https://portfolio.metamask.io/
X402_NETWORK=base-sepolia
X402_FACILITATOR_URL=https://x402.org/facilitator
X402_VERIFICATION_TIMEOUT=30000
X402_ENDPOINT_PRICE_USD=0.01

CDP_API_KEY_ID=""
CDP_API_KEY_SECRET=""
                  # ^ KEYS taken from -> https://portal.cdp.coinbase.com/projects/api-keys

# Set if you'd like to use the Fire Engine closed beta
FIRE_ENGINE_BETA_URL=

# Supabase setup (used to support DB authentication, advanced logging, etc.)
# Note: DB authentication is currently not supported for self-hosted instances.
SUPABASE_ANON_TOKEN=
SUPABASE_URL=
SUPABASE_SERVICE_TOKEN=
